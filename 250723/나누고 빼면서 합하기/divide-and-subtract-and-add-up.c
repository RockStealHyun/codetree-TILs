#include <stdio.h>

int arr[101];
int cnt;

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 1; i <= n; i++)
        scanf("%d", &arr[i]);
    cnt = m;
    // Please write your code here.
    return 0;
}